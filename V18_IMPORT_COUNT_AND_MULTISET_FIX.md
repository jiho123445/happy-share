# V18 Excel Import Count & Multiset Fix

## 핵심 수정
- 실제 Excel에서 읽은 행 수와 저장 후 누적 행 수를 화면에 표시합니다.
- 기존 sourceKey가 있는 자료도 내용 fingerprint의 "개수"에 포함합니다.
- 같은 실제 후원건이 기존에 2건이면 Excel의 동일 행 2건까지 중복으로 처리합니다.
- Excel에 동일한 후원행이 3건 있으면 3번째까지 새 행으로 보존합니다.
- incoming 데이터끼리는 중복 제거하지 않습니다.
- 파일명을 바꾸어 재업로드해도 실제 후원행이 동일하면 중복으로 처리할 수 있습니다.
- `period` 빈값은 undefined가 아니라 빈 문자열로 저장하여 Firestore undefined 오류를 방지합니다.

## 이번에 제공된 실제 Excel 검사
업로드된 `너브내행복나눔재단_후원금자료_표준서식 (8월)(1).xlsx`는
`후원금자료양식` 시트 1개이며, 헤더 제외 데이터 행은 7건입니다.
김지호 데이터는 1건이며, 전체 후원금 합계는 1,300,000원입니다.

따라서 이 파일 자체는 이전 화면에서 보였던 "김지호 3건 중 2건" 문제를 재현하는 파일은 아닙니다.
이번 V18에는 어느 단계에서 행이 줄어드는지 즉시 확인할 수 있도록 업로드 결과 카운터를 추가했습니다.
