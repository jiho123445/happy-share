# v14 후원내역 3건 중 2건만 표시되는 데이터 손실 수정

## 문제 원인

기존 `src/utils/donationDedup.ts`의 `getPaymentFingerprint()`가 후원일자의 **월(YYYY-MM)**만 사용했습니다.

따라서 같은 후원자가 같은 달에 같은 금액을 같은 방법/내용으로 여러 번 후원하면 서로 다른 후원건도 같은 fingerprint가 생성되었습니다.
`Map<string, number>`가 마지막 한 건만 가리키면서 다음 행이 기존 행에 병합되어, Excel에는 3건이 있어도 화면/Firestore에는 2건만 남을 수 있었습니다.

## v14 수정 내용

- 정확한 후원일자 `YYYY-MM-DD`를 중복 식별키에 포함
- 같은 fingerprint가 여러 건 존재할 수 있도록 내부 인덱스를 `number[]`로 변경
- 보강 병합은 후보가 **정확히 1건**일 때만 수행
- 후보가 여러 개이거나 식별이 불확실하면 새 후원건으로 그대로 보존
- 같은 달의 서로 다른 날짜 후원건은 절대로 월 단위로 합치지 않음
- 기존 v13에서 누락된 후원건은 원본 Excel 3건을 다시 업로드하면 정상적으로 추가되도록 함
- Firestore 문서 ID도 정확한 날짜를 반영하도록 변경

## 확인 방법

1. 수정본 배포
2. 관리자 로그인
3. 원본 Excel 파일 전체를 다시 업로드
4. Excel 관리 화면에서 `누적 후원자료` 건수 확인
5. 기부금영수증 탭에서 동일 후원자의 2026년 상세 후원내역이 3건인지 확인

기존에 이미 잘못 누락된 데이터는 코드만 교체한다고 자동 복구되지 않습니다. **원본 Excel을 다시 한 번 업로드해야 합니다.**
