# V16 - Firebase 원본 기준 Excel 누적/초기화 문제 수정

## 핵심 문제
기존 버전은 Excel 업로드 시 현재 화면의 React state(`donations`)를 기존자료로 사용했습니다.
따라서 `회원 명단 초기화`를 누른 뒤에는 화면 state가 `[]`가 되어 Firebase에 이미 저장된 자료를
기준으로 중복/누적 처리를 할 수 없었습니다.

## V16 수정
1. 로그인된 Firebase 모드에서는 Excel 업로드 직전에 `loadCloudDonations()`로 최신 Cloud 자료를 다시 읽습니다.
2. 화면 초기화는 React state만 비우며 Firestore `donations` 컬렉션은 삭제하지 않습니다.
3. Excel 3건 / Firebase 2건이면 기존 2건을 유지하고 부족한 1건을 추가하여 최종 3건이 되도록 합니다.
4. Firebase가 원본이며 화면 state는 표시용 캐시로 취급합니다.
5. 샘플 데이터는 초기 화면에 자동 표시하지 않고 '샘플 데이터 불러오기' 버튼을 눌렀을 때만 표시합니다.
6. 화면 버튼/확인 문구를 '회원 명단 초기화'가 아닌 '화면 명단 초기화'로 명확하게 변경했습니다.

## 데이터 삭제에 대한 주의
V16의 '화면 명단 초기화'는 Firestore 데이터를 삭제하지 않습니다.
Firestore 전체 삭제 기능은 별도로 제공하지 않습니다.
