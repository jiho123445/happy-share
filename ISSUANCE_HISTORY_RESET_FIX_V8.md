# 발급내역 초기화/연도 선택 표시 수정 v8

- 발급내역 `초기화`는 Firestore/LocalStorage의 실제 발급 데이터를 삭제하지 않고 화면 표시만 초기화합니다.
- 초기화 상태를 `sessionStorage`에 보관하여 발급내역 → 영수증 발급 → 발급내역으로 이동해도 초기화 상태가 유지됩니다.
- 초기화 후에는 `전체 연도` 상태에서 발급내역이 다시 자동 표시되지 않습니다.
- 사용자가 실제 연도(예: 2026년도)를 선택하면 그 연도의 발급내역만 다시 표시됩니다.
- `내역 다시 불러오기`를 누르면 전체 연도가 다시 표시됩니다.
- 상단 총 발급 건수/총 발급 금액도 선택 연도에 맞춰 계산됩니다.
- 실제 Firebase 발급자료는 초기화 버튼으로 삭제되지 않습니다.
