# V21 기부금영수증 PDF 공유 기능 수정

이번 버전은 기존 Excel/Firebase/영수증 발급 로직을 변경하지 않고, 발급된 영수증 미리보기의 **공유 기능만 추가/개선**했습니다.

## 변경 사항
- 영수증 미리보기 상단에 `공유` 버튼 추가
- 카카오톡/문자 탭과 이메일 탭을 제공하는 `기부금영수증 발송 & 공유 마법사` 추가
- 기존 PDF 생성 로직을 재사용하여 실제 `File(application/pdf)` 객체 생성
- 브라우저가 Web Share API의 파일 공유를 지원하면 PDF를 실제 첨부파일로 공유
- 모바일에서는 공유 대상에서 카카오톡/이메일 등 설치된 앱을 선택할 수 있음
- 파일 공유가 지원되지 않는 PC 브라우저에서는 PDF 다운로드 + 안내 문구 복사/메일 작성창 열기 방식으로 안전하게 fallback
- 이메일 `mailto:` fallback에서는 브라우저 보안상 PDF 자동 첨부가 불가능하므로 사용자가 다운로드된 PDF를 직접 첨부하도록 명확히 안내
- 기존 PDF 저장/인쇄 기능은 유지

## 중요
웹 브라우저가 설치된 카카오톡/메일 프로그램에 임의로 파일을 강제 첨부하는 것은 보안상 허용되지 않습니다. 따라서 가능한 환경에서는 Web Share API를 사용하고, 불가능한 환경에서는 다운로드/작성창 방식으로 처리합니다.
