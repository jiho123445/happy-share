# v13 후원금액 덮어쓰기 / 동명이인 오병합 / 검색화면 오표기 수정

이번 버전은 지금까지의 v5~v12 수정과는 별개로 남아있던 **가장 심각한 데이터 훼손 버그**를 포함해
아래 3가지 문제를 수정했습니다.

## 1. [치명적] 엑셀 헤더 인식 오류로 후원금액이 덮어써지는 버그 (src/utils/excelParser.ts)

### 증상
표준 9열 서식(성명/주민번호/주소/후원일자/**후원금액**/후원방법/**기부금유형**/**기부금코드**/기부내용)을
업로드하면, `기부금유형`과 `기부금코드` 열이 실수로 `후원금액` 열로 잘못 인식되어
실제 후원금액(예: 100,000원)이 기부금코드 값(예: "40") 등으로 덮어써지는 문제가 있었습니다.
`downloadSampleExcelTemplate()`이 만들어주는 공식 샘플 서식 자체가 이 문제를 그대로 유발했습니다.

### 원인
`findMatchingField()`가 헤더 이름을 완전일치와 부분일치를 같은 우선순위로 검사했습니다.
`후원금액` 필드의 동의어 목록에 있는 짧은 단어 `'기부금'`이 `기부금유형`, `기부금코드`라는
헤더 문자열에도 포함되어 있어서, 실제로는 `기부금유형` 완전일치 동의어가 `donationType`에 등록되어
있음에도 부분일치가 먼저 걸려버리는 순서 문제였습니다.

### 수정
- 헤더 매칭을 2단계로 변경: **완전일치를 항상 최우선**으로 검사하고, 완전일치가 없을 때만
  부분일치를 검사하되 **더 길고 구체적인 동의어가 이기도록** 함
- 같은 필드에 두 개 이상의 열이 매칭되는 경우, 가장 왼쪽(첫) 열만 채택하고 이후 열은 무시하는
  안전장치 추가 (향후 동의어가 늘어나도 조용한 덮어쓰기 재발 방지)

### 검증
표준 서식 헤더 재테스트 결과:
```
기부금유형 -> donationType (수정 전: amount)
기부금코드 -> donationCode (수정 전: amount)
```
기존 v9에서 목표했던 "헤더명이 약간 달라도 인식" 기능(예: `기부구분`, `기부코드`, `이체방법` 등)은
그대로 유지됩니다.

### ⚠️ 기존에 저장된 데이터 관련 안내
이 버그가 있는 버전으로 `기부금유형`/`기부금코드` 열이 포함된 엑셀을 업로드한 적이 있다면,
Firestore `donations` 컬렉션에 저장된 해당 건들의 `amount` 값이 잘못 저장되어 있을 수 있습니다.
v13 적용 후에는 **원본 엑셀 파일을 다시 업로드**하여 기존 납부건을 보강(enrich)하시길 권장합니다.
(donationDedup 로직상 같은 납부건으로 인식되면 올바른 금액으로 갱신됩니다.)

## 2. 주민/사업자번호가 둘 다 없을 때 이름만으로 동명이인이 잘못 병합될 위험 (src/utils/donationDedup.ts)

### 증상
같은 이름을 가진 서로 다른 사람의 후원 자료가, 양쪽 모두 주민/사업자번호가 비어있는 경우
자동으로 같은 사람의 자료로 병합될 수 있었습니다.

### 수정
`samePersonAndPayment()`에서 양쪽 다 식별번호가 없을 때는 주소가 둘 다 입력되어 있는 경우
**주소까지 일치해야만** 동일인으로 인정하도록 보강했습니다. (주소가 한쪽이라도 비어 있으면
기존처럼 이름만으로 판단 — 정상적인 정보 보강 시나리오는 그대로 동작)

## 3. 검색화면에서 이름이 다른 사람도 "동명이인"으로 잘못 표시되는 문제 (src/components/DonorSearch.tsx)

### 증상
후원자 검색 시 이름 일부만 포함되어도 검색되는데(예: "철수" 검색 시 "김철수", "박철수민" 모두 검색됨),
이 부분일치 결과를 전부 "동명이인"으로 표시해 혼란을 줬습니다.

### 수정
- 이름이 **완전히 같은** 후원자가 2명 이상일 때만 "동명이인" 문구/스타일 사용
- 이름이 정확히 같지 않은 부분일치 검색결과는 "검색결과"라는 별도 문구로 구분 표시
- (부가 수정) 부분일치 검색결과가 정확히 1명뿐인데 화면에 아무것도 표시되지 않던
  엣지 케이스도 함께 수정하여 해당 1명이 바로 선택되도록 함
